# Test-only dependencies. Kept out of requirements.txt so they are not baked
# into the production container image.
-r requirements.txt

# >=9.0.3 clears PYSEC-2026-1845.
pytest>=9.0.3,<10.0.0
# Required by fastapi.testclient.TestClient — the suite fails to import without it.
httpx>=0.27.0,<1.0.0
