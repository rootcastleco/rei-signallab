# Production API Base URL (Leave empty for relative /api/** calls routed via Firebase Hosting proxy)
VITE_API_BASE_URL=

# Expected backend version for handshake verification
VITE_EXPECTED_API_VERSION=2.1.0
