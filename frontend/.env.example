# Production API Base URL (Leave empty for relative /api/** calls routed via Firebase Hosting proxy)
VITE_API_BASE_URL=

# Expected backend application release version (e.g. 2.1.0)
VITE_EXPECTED_APP_VERSION=2.1.0

# Expected backend API version (e.g. v1)
VITE_EXPECTED_API_VERSION=v1
